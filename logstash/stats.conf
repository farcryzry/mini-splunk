input {
	exec {
	    codec => "plain"
	    command => "ifconfig eth0 | grep 'inet addr:' | cut -d: -f2 | awk '{print $1}' | tr '\n' ' ' && top -bn 1 | tail -n +8 | awk '{s+=$9} END {print s}'"
	    interval => 5
	    type => "cpu"
	}

	exec {
	    codec => "plain"
	    command => "ifconfig eth0 | grep 'inet addr:' | cut -d: -f2 | awk '{print $1}' | tr '\n' ' ' && free -t | grep 'buffers/cache' | awk '{print $3/($3+$4) * 100}'"
	    interval => 5
	    type => "memory"
	}

	exec {
	    codec => "plain"
	    command => "ifconfig eth0 | grep 'inet addr:' | cut -d: -f2 | awk '{print $1}' | tr '\n' ' ' && sar -b 1 10 | grep 'Average:' | awk -F ' ' '{print $2} {print $5} {print $6}'a | tr '\n' ' '"
	    interval => 30
	    type => "io"
	}

	exec {
	    codec => "plain"
	    command => "ifconfig eth0 | grep 'inet addr:' | cut -d: -f2 | awk '{print $1}' | tr '\n' ' ' && ps -eLf | wc -l"
	    interval => 5
	    type => "thread"
	}
}

filter{
    grok {
	type => "cpu"
        match => ["message", "%{IP:ip}%{SPACE}%{NUMBER:cpu-usage:float}"]
    }

    grok {
	type => "memory"
        match => ["message", "%{IP:ip}%{SPACE}%{NUMBER:memory-usage:float}"]
    }

    grok {
	type => "io"
        match => ["message", "%{IP:ip}%{SPACE}%{NUMBER:trans-per-sec:float}%{SPACE}%{NUMBER:read-Bps:float}%{SPACE}%{NUMBER:wrtn-Bps:float}"]
    }

    grok {
	type => "thread"
        match => ["message", "%{IP:ip}%{SPACE}%{NUMBER:thread-total:int}"]
    }

    mutate {
	add_field => {"vm_name" => "Lan"}
        remove_field => [ "host", "command", "@version" ]
    }
}
output {
  stdout { codec => rubydebug }
  file {
        path => "./stats.log"
  }
  mongodb {
        codec => "plain"
        collection => "stats"
        database => "logstash"
        generateId => false
        isodate => false
        retry_delay => 3
        uri => "mongodb://10.189.1.165:27017"
        workers => 1
  }
  elasticsearch {
    protocol => "http"
    codec => "plain"
    host => "10.189.1.165"
    port => 9200
    workers => 1
  }
}
